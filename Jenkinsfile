pipeline {
    agent any
       environment {
       color = "blue"
   }
    stages {
        stage('GIT checkout1') {
            steps {
                echo 'Checkout from SCM'
            }
        }
    }
}
